import { BindSelectDirective } from './bind-select.directive';

describe('BindSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new BindSelectDirective();
    expect(directive).toBeTruthy();
  });
});
